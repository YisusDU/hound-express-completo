import "styled-components";
import { DefaultTheme } from "styled-components";

const Theme: DefaultTheme = {
  colors: {
    primaryColor: "#091e3f",
    secondaryColor: "#6ac6de",
    darkModeBg: "#010d21b0",
  },
  fonts: {
    base: "Open Sans, sans-serif",
  },
};

export default Theme;
